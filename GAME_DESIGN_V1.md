# 大模型三国回合制游戏设计文档（V1 历史稿）

> 状态：第一版草案  
> 目标：验证“大模型扮演三国势力并自主决策”是否能产生有趣、可理解、可重复运行的对局。

## 1. 游戏定位

玩家创建一局游戏，为多个三国势力分别指定大模型。每个模型只负责：

- 阅读己方可见的局势。
- 以所扮演人物的立场进行推理。
- 提交符合结构化协议的行动。
- 生成外交发言和简短的决策理由。

程序是唯一裁判，负责保存状态、校验行动、扣除资源、结算战争和生成回合记录。模型不能直接修改数值，也不能自行宣布行动成功。

玩家可以选择：

- 纯观看模式：全部势力由模型控制。
- 人工参与模式：玩家接管其中一个势力，其余势力仍由模型控制。

## 2. 第一版设计原则

### 2.1 核心原则

- **规则简单，决策不简单**：资源和动作数量少，但彼此存在明确约束。
- **程序负责事实，模型负责意图**：所有数值变化必须来自游戏引擎。
- **先采用完全信息**：第一版公开各势力的资源和兵力，先验证策略本身，暂不加入战争迷雾。
- **行动具有机会成本**：每回合只能执行一个主要行动，不能同时发展、征兵和进攻。
- **扩张伴随成本**：城池越多产出越高，但需要分兵、防守和消化新领土。
- **失败可以解释**：每次非法行动、资源不足和战争结果都必须能输出明确原因。

### 2.2 第一版暂不包含

- 武将招募、单挑、技能和装备。
- 兵种克制、科技树和复杂地形。
- 复杂地图界面和自由行军路线。
- 间谍、暗杀、隐藏兵力等秘密系统。
- 模型自行发明的特殊技能或临时规则。

## 3. 可借鉴的战棋设计

第一版只吸收成熟游戏中的基础约束，不复制复杂系统：

- **《三国志》系列**：城池产出、征兵、内政、外交和攻城构成主要循环。
- **《文明》系列**：发展行为需要放弃当回合的军事机会，长期收益与短期安全形成取舍。
- **《Risk》类领土游戏**：领土带来资源，也会扩大边界和防守压力。
- **《Diplomacy》类外交游戏**：提案必须得到对方接受，语言可以影响决策，但不能绕过规则。
- **战棋游戏**：移动范围、城防和明确的结算顺序减少争议。

## 4. 最小游戏对象

### 4.1 游戏 `Game`

- `game_id`：游戏唯一标识。
- `tenant_id`：所属租户。
- `turn`：当前回合。
- `max_turns`：最大回合数，建议第一版为 30。
- `status`：等待、进行中、已结束。
- `ruleset_version`：规则版本，保证旧对局可以复盘。
- `resolution_seed`：仅在未来启用随机规则时使用；第一版战争不使用随机数。

### 4.2 势力 `Faction`

- `faction_id`：势力唯一标识。
- `leader_name`：曹操、刘备、孙权等角色。
- `persona`：角色目标、性格和表达风格。
- `model_config_id`：绑定的模型配置，只保存引用，不保存 API Key。
- `gold`：金钱。
- `food`：粮草。
- `alive`：是否仍拥有城池。
- `capital_city_id`：首都，仅用于展示；第一版首都失守不直接淘汰。

### 4.3 城池 `City`

- `city_id`：城池唯一标识。
- `owner_faction_id`：当前所属势力。
- `adjacent_city_ids`：相邻城池，只能在相邻城池之间移动或进攻。
- `gold_income`：每回合金钱产出。
- `food_income`：每回合粮草产出。
- `manpower`：当前可征兵源。
- `manpower_capacity`：兵源上限。
- `manpower_recovery`：每回合恢复的兵源。
- `garrison`：驻守兵力。
- `defense_level`：城防等级，建议范围为 0～3。
- `occupation_turns`：新占领状态剩余回合数。

城池之间只需要维护一张简单的连接表，前端用文字显示“可进攻目标”，不需要绘制地图。

### 4.4 外交关系 `DiplomaticRelation`

- `faction_a_id`、`faction_b_id`：关系双方。
- `status`：中立、同盟、宣战。
- `remaining_turns`：同盟剩余有效回合。
- `break_pending`：是否已提出解除同盟。

### 4.5 回合命令 `Order`

- `game_id`、`turn`、`faction_id`：命令归属。
- `action_type`：行动类型。
- `parameters`：目标城池、数量等结构化参数。
- `public_message`：角色扮演发言。
- `reason_summary`：简短决策摘要。
- `validation_result`：是否合法及失败原因。
- `resolution_result`：最终结算结果。

## 5. 资源系统

### 5.1 必要资源

| 资源 | 主要来源 | 主要消耗 | 约束作用 |
| --- | --- | --- | --- |
| 金钱 | 城池商业产出 | 征兵、发展、修筑城防 | 防止无成本扩军和无限发展 |
| 粮草 | 城池农业产出 | 军队维持、征兵、出征 | 限制军队规模和连续战争 |
| 兵源 | 城池缓慢恢复 | 征兵 | 防止只靠囤积金钱瞬间暴兵 |
| 兵力 | 征兵获得 | 维持、战斗损失、守城 | 用于进攻，也必须分散防守 |
| 城池 | 战争占领 | 需要驻军和消化 | 提供长期产出，同时扩大防线 |
| 行动机会 | 每回合固定一次 | 所有主要行动 | 防止一回合完成整套发展与战争 |

`兵源`虽然增加了一个数值，但它是防止经济滚雪球转化为无限兵力的必要约束，第一版建议保留。

### 5.2 资源约束闭环

```text
城池 ──产出──> 金钱、粮草、兵源
金钱 + 粮草 + 兵源 ──征兵──> 兵力
兵力 ──每回合消耗──> 粮草
兵力 + 额外粮草 ──出征──> 争夺城池
占领城池 ──短期低产出、需要驻军──> 延迟扩张收益
发展城池 ──消耗本回合行动和金钱──> 提高未来产出
```

这个闭环形成以下取舍：

- 军队太少会被进攻，军队太多会拖垮粮草。
- 连续征兵会耗尽兵源，不能只靠经济优势无限扩军。
- 发展可以形成长期优势，但会牺牲当回合的军事行动。
- 攻城需要额外粮草，连续战争必须提前准备。
- 新城不能立即贡献全部资源，降低滚雪球速度。
- 兵力驻扎在具体城池，扩张后必须分兵，不能让同一支军队同时防守所有领土。

## 6. 初始数值建议

数值仅作为原型起点，后续应通过自动对局统计调整。

### 6.1 对局规模

- 3 个势力。
- 6～7 座城池，每个势力初始拥有 2 座城池，可增加 1 座中立城池。
- 城池使用固定连接关系，每座城连接 2～3 座城。
- 第一版采用机械对称开局：各势力资源总量、城池产出和连接数量尽量一致。

角色差异只体现在模型、提示词和行为风格，不在第一版给予曹操、刘备、孙权不同的数值加成。

### 6.2 初始资源

每个势力建议：

- 金钱：20。
- 粮草：30。
- 每座城池驻军：8～10 个兵力单位。
- 每座城池兵源：6，上限 10；第 1～20 回合每回合恢复 2，第 21～30 回合恢复 1，第 31 回合起不再恢复。
- 每座城池基础产出：金钱 4、粮草 6。
- 每个兵力单位可在展示层解释为 100 名士兵，结算层只使用整数单位。

## 7. 每回合行动

每个势力每回合拥有：

- **一个主要行动**。
- **一个可选外交提案或公开发言**。
- 对其他势力外交提案进行接受或拒绝，不占用主要行动。

### 7.1 发展商业 `develop_commerce`

- 目标：己方一座城池。
- 成本：8 金钱。
- 效果：该城每回合金钱产出永久增加 2。
- 上限：每座城最多发展 3 次。
- 约束：新占领状态下不能发展。

回本至少需要 4 回合，避免发展成为没有代价的即时收益。

### 7.2 发展农业 `develop_agriculture`

- 目标：己方一座城池。
- 成本：8 金钱。
- 效果：该城每回合粮草产出永久增加 3。
- 上限：每座城最多发展 3 次。
- 约束：新占领状态下不能发展。

### 7.3 征兵 `recruit`

- 目标：己方一座城池。
- 每征募 1 个兵力单位消耗：2 金钱、1 粮草、1 兵源。
- 单次最多征募 5 个兵力单位。
- 新兵直接加入目标城驻军。
- 实际征募量不得超过金钱、粮草、兵源和单次上限共同允许的数量。

程序不自动缩减模型提交的数量。资源不足时整个命令判定为非法，避免模型利用模糊结算试探边界。

### 7.4 修筑城防 `fortify`

- 目标：己方一座城池。
- 成本：6 金钱、4 粮草。
- 效果：城防等级增加 1。
- 上限：城防等级最高为 3。
- 新占领状态下不能修筑。

城防有效但有上限，防止无限龟缩。

### 7.5 调兵 `transfer`

- 来源和目标必须是相邻的己方城池。
- 来源城必须至少保留 1 个兵力单位。
- 调动不额外消耗粮草，但占用本回合主要行动。

调兵作为独立行动，使兵力部署成为真实机会成本。

### 7.6 出征 `attack`

- 来源：己方城池。
- 目标：与来源相邻且不属于己方的城池。
- 来源城必须至少保留 1 个兵力单位。
- 最少投入 2 个兵力单位。
- 出征额外消耗粮草：每投入 1 个兵力单位消耗 1 粮草。
- 不能进攻仍处于有效同盟关系的势力。

粮草在战争结算前扣除。即使战败，已经消耗的出征粮草也不返还。

### 7.7 休整 `rest`

- 无成本、无额外收益。
- 当模型主动保留资源或命令非法时使用。

非法命令统一降级为休整，不能让模型反复修改命令直到碰到一个合法边界。

## 8. 自动收入与军队维持

每回合开始时：

1. 城池按当前回合的恢复档位恢复兵源，不超过上限。
2. 城池产生金钱和粮草。
3. 新占领城池在占领期只产生 50% 的金钱和粮草，向下取整。
4. 每 4 个兵力单位消耗 1 粮草作为维持费用，向上取整。

粮草不足时按固定规则处理，模型不能选择性规避：

- 粮草降为 0。
- 每缺少 1 粮草，损失 1 个兵力单位。
- 损失从驻军最多的城池开始，数量相同时按固定城池 ID 排序。

维持费用让“大量囤兵”不再是无条件最优策略；固定裁减顺序保证结果可复现。

## 9. 战争结算

### 9.1 第一版原则

- 不使用随机数，便于调试、复盘和公平比较模型。
- 规则和公式对所有参与者公开。
- 模型只提交来源、目标和投入兵力。
- 城防只增强守方，不直接制造额外伤害。

### 9.2 建议公式

```text
攻击战力 = 投入兵力
防御战力 = 守军 × (1 + 城防等级 × 20%)
```

- 常规阶段中，攻击战力达到防御战力的 120% 时，进攻方占领城池。
- 仅剩两个存活势力时进入决战阶段，占领阈值降为防御战力的 105%。
- 未达到占领阈值时守方守住城池；若攻击战力至少达到防御战力的 50%，城防额外降低 1 级。
- 失败方损失自身参战兵力的 60%。
- 胜利方损失自身参战兵力的 20%～40%，具体比例随双方战力接近程度线性增加。
- 所有损失向上取整，城池驻军最低不会因取整变为负数。
- 进攻胜利后，剩余进攻部队进入目标城池。
- 防守胜利后，剩余守军继续驻守；剩余进攻部队返回来源城。

胜方损失比例建议采用：

```text
胜方损失率 = 20% + 20% × (败方有效战力 / 胜方有效战力)
```

因此碾压战也有最低损失，势均力敌的战斗会显著消耗双方兵力。

### 9.3 占领约束

- 新占领城池进入 2 回合占领状态。
- 新占领城池不在占领当回合末减少计数，从下一回合末开始递减。
- 占领期产出减半。
- 占领期不能发展商业、农业或城防。
- 城防等级在被占领后降低 1，最低为 0。
- 城内兵源保留，但不能超过当前上限。

这些规则让扩张获得长期收益，但不能在占领后立即滚动形成下一轮完整产出。

## 10. 回合结算顺序

第一版建议所有势力同时读取同一份回合快照并提交命令，避免后行动者看到本回合新结果。

1. **回合开始**：恢复兵源、结算城池收入、扣除军队维持费用。
2. **生成观察信息**：为每个势力生成只读局势快照。
3. **收集命令**：模型或玩家提交一个主要行动及可选外交内容。
4. **锁定命令**：截止后不允许重新提交。
5. **合法性校验**：使用回合快照检查资源、归属、相邻关系和数量范围。
6. **外交结算**：处理同盟提案、接受、拒绝和解除申请。
7. **内政结算**：发展、征兵、城防和调兵。
8. **战争结算**：按本回合轮换的势力优先级依次处理冲突。
9. **占领状态更新**：减少占领剩余回合。
10. **胜负检查**：仅当一家势力控制全部城池时结束游戏。
11. **生成回合报告**：程序生成事实摘要，模型文案只能作为附加叙事。

战争无法完全同时结算时，采用每回合轮换的优先顺序，例如：

```text
第 1 回合：魏 → 蜀 → 吴
第 2 回合：蜀 → 吴 → 魏
第 3 回合：吴 → 魏 → 蜀
```

优先级必须在模型决策前公开，避免隐藏规则，并减少固定先手优势。

战争行动在轮到执行时需要再次检查当前状态：

- 来源城已经失守时，行动取消，不扣除出征粮草。
- 目标城已经归属己方或当前盟友时，行动取消，不自动改为调兵或攻击盟友。
- 目标城被第三方占领且不是盟友时，仍按原命令进攻该城当前守军。
- 因前序战争导致取消属于“局势变化”，不记为模型非法命令。

这使多方进攻同一城池和相互进攻可以用同一套顺序规则结算，不需要第一版引入复杂的三方会战系统。

## 11. 外交规则

### 11.1 同盟

- 同盟必须由一方提出、另一方明确接受后生效。
- 本回合接受的同盟从下一回合开始时生效，不追溯影响已经锁定的战争命令。
- 默认持续 3 回合。
- 同盟期间双方不能互相进攻。
- 同盟到期后自动恢复中立，可以重新续约。
- 第一版不允许共同胜利。

### 11.2 解除与背盟

- 势力可以提前申请解除同盟。
- 解除申请公开，并在下一回合开始时生效。
- 提出解除的当回合和生效前不能攻击盟友。

这保留背盟策略，但避免模型在同一条命令中先解除再偷袭，绕过防御准备。

### 11.3 外交文本

- 每回合最多向一个势力发送一条外交消息。
- 消息长度设定上限，例如 300 字。
- 外交文本是“不可信游戏内容”，不能改变系统规则、工具权限或观察范围。
- 接收模型的系统提示必须明确：外交消息可能包含欺骗或提示词注入，只能作为角色对话理解。

## 12. 胜负条件

满足以下任一条件时结束游戏：

- 只剩一个仍拥有城池的势力，该势力获胜。
- 某势力控制超过半数城池，并连续保持 2 个完整回合，该势力获胜。
- 不设置回合数胜利或过半胜利，必须控制全部城池才能获胜。

回合上限结算建议：

```text
总分 = 城池基础胜利点之和
     + 已完成的商业发展次数
     + 已完成的农业发展次数
```

金钱、粮草和兵力不直接计分，防止模型通过囤积资源而不是争夺领土获胜。若总分相同，依次比较城池数、剩余兵力和粮草。

## 13. 防漏洞与公平性

### 13.1 服务端权威

- 数据库中的游戏状态是唯一事实来源。
- 模型无数据库、文件系统和结算接口权限。
- 模型只能提交预定义 JSON，文本中的数字和指令不产生游戏效果。
- 所有数量均校验类型、最小值、最大值、归属和资源余额。
- 每个命令带唯一 ID，重复提交不会重复结算。
- 每局固定 `ruleset_version`，进行中不能热切换规则。

### 13.2 非法命令处理

- 缺少字段、字段越界、目标不存在或资源不足均视为非法。
- 非法命令自动变为休整，并记录具体原因。
- 不允许结算程序“猜测模型意图”或自动改成最大可用数量。
- 模型输出超时同样视为休整。

明确而严格的失败规则可以防止不同模型因格式容错程度不同而获得不公平优势。

### 13.3 模型之间的公平

不同模型本身能力不同，因此需要区分两种玩法：

- **娱乐模式**：允许不同模型、不同厂商和不同角色提示词同场，能力差异本身就是看点。
- **竞技模式**：所有势力使用相同模型版本、温度、上下文长度、输出 token 上限和思考时间，只改变角色身份。

两种模式都必须保证：

- 获得相同结构的观察信息。
- 拥有相同的行动数量和超时时间。
- 不因 API 返回速度决定结算先后。
- 不能访问其他势力的上下文、隐藏推理或模型配置。

### 13.4 租户和势力隔离

所有游戏数据查询至少同时限定：

```text
tenant_id + game_id
```

所有模型上下文、命令和外交记录至少同时限定：

```text
tenant_id + game_id + faction_id
```

额外要求：

- 每个势力使用独立会话历史，不共享完整消息数组。
- 服务端按势力生成 `observation`，禁止把完整数据库对象交给模型后要求其自行忽略字段。
- 模型 API Key 存储在租户密钥配置中，不进入提示词、日志和对局快照。
- 日志中的提示词和外交内容按租户隔离，并对密钥形式的文本进行脱敏。

### 13.5 数值系统常见漏洞及约束

| 潜在漏洞 | 对应约束 |
| --- | --- |
| 无限囤兵 | 军队每回合消耗粮草 |
| 有钱即可瞬间暴兵 | 征兵同时消耗兵源，且有单次上限 |
| 占城后快速滚雪球 | 新占领城池两回合产出减半且不能发展 |
| 一支军队同时防守全部城池 | 兵力归属具体城池，只能相邻调动 |
| 无限堆城防导致僵局 | 城防等级封顶，且胜利按领土结算 |
| 两强长期反复征兵 | 第 21 回合起削减兵源恢复，第 31 回合起停止恢复，并启用决战规则 |
| 同回合发展、征兵、进攻 | 每回合只有一个主要行动 |
| 先签盟再立即背刺 | 解除同盟延迟一回合生效 |
| 模型虚构特殊能力 | 只接受固定行动枚举和结构化参数 |
| 反复提交试探资源边界 | 命令锁定，非法命令直接休整 |
| 后提交者利用已结算信息 | 所有模型基于同一回合快照提交 |
| 固定先手长期占优 | 战争结算优先级每回合轮换 |
| 外交消息提示词注入 | 将外交文本标记为不可信内容并限制长度 |

## 14. 模型观察信息

第一版采用完全信息，模型每回合收到相同格式的数据：

```text
角色：刘备，蜀汉势力
当前回合：8 / 30
本回合战争结算优先级：蜀 → 吴 → 魏

己方：
- 金钱、粮草
- 每座城的产出、兵源、驻军、城防、相邻城池
- 当前外交关系

其他势力：
- 金钱、粮草
- 城池、驻军、城防和相邻关系
- 当前外交关系

上一回合公开事件：
- 合法行动及结算结果
- 城池归属变化
- 外交提案和公开发言

本回合可选行动：
- 根据当前资源动态生成的合法行动及参数范围
```

程序应尽量直接给出“当前合法选项”，减少模型因算术或格式错误浪费回合，但模型仍需自行选择具体目标和数量。

## 15. 模型决策协议

建议统一输出：

```json
{
  "action": {
    "type": "attack",
    "source_city_id": "xinye",
    "target_city_id": "xiangyang",
    "troops": 8
  },
  "diplomacy": {
    "target_faction_id": "wu",
    "type": "message",
    "message": "曹军势大，愿与江东共拒北军。"
  },
  "public_message": "匡扶汉室，当先取荆襄。",
  "reason_summary": "襄阳守军较少，且夺取后可以缩短北方战线。"
}
```

约束：

- `reason_summary` 只要求简短的可展示理由，不要求暴露模型完整思维链。
- 玩家看到的是行动、公开发言、理由摘要和程序结算结果。
- JSON 之外的内容不参与结算。

## 16. 第一版验证指标

实现后应通过批量自动对局观察：

- 平均对局回合数是否处于 12～25 回合。
- 是否经常出现粮草归零和无脑暴兵。
- 发展、征兵、调兵、城防、进攻是否都有实际使用。
- 是否存在某个行动在多数局面下始终最优。
- 先手和不同初始位置的胜率是否明显偏高。
- 占领第一座敌城后是否几乎必然获胜。
- 非法命令和超时比例是否可接受。
- 回合报告能否让观看者理解模型为何成功或失败。

出现以下情况时优先调整数值，而不是增加新系统：

- 对局长期僵持：降低城防收益、提高回合末领土价值或降低进攻粮草成本。
- 扩张滚雪球过快：延长占领期、增加军队维持或降低新城产出。
- 所有模型只征兵：提高维持成本或降低单次征兵上限。
- 所有模型只发展：降低发展收益或增加外部军事压力。
- 外交没有作用：延长同盟期限或让多线防守成本更明显。

## 17. 后续迭代顺序

第一版稳定后，建议按以下顺序逐项增加，而不是同时扩展：

1. 敌方兵力只显示区间，加入轻量战争迷雾。
2. 增加武将作为有限数量的战力修正资源。
3. 增加一到两种兵种克制。
4. 增加可被程序验证的计谋行动。
5. 增加历史剧本、非对称开局和角色专属目标。
6. 增加对局回放、模型切换和统计排行榜。

每次新增系统都必须回答三个问题：它消耗什么资源、被什么规则克制、是否创造了唯一最优策略。
